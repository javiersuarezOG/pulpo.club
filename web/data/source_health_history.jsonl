# source_health_history.jsonl — one row per source per nightly run
# Schema: {ts, source, status, count, duration_s, error_class, error_msg, max_pages_hit, limit_hit}
# Bootstrapped empty so consumers can rely on the file existing.
