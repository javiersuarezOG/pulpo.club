/// <reference types="vite/client" />

// Compile-time constant injected by Vite (see vite.config.js).
// `false` on the production deploy so the dev panel gets dead-code-eliminated;
// `true` everywhere else (vite dev, Vercel preview deploys).
declare const __PULPO_DEV_PANEL__: boolean;
