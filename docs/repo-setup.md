# Repo setup checklist

Apply these once in the GitHub UI after the first push.

- [ ] Settings → Branches → protect `main`: require status check `ci`, require Code Owners review.
- [ ] Settings → General → enable "Allow rebase merging" + "Allow squash merging", disable "Allow merge commits".
- [ ] Settings → General → enable "Automatically delete head branches".
